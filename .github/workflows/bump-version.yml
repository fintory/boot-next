name: Bump version
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name:  'Automated Version Bump'
      uses:  'phips28/gh-action-bump-version@master'
      with:
        tag-prefix:  ''
